# iStudySpot 后端 API 测试

## 测试环境准备

### 1. 启动后端服务

在运行测试之前，需要确保后端服务已经启动。可以通过以下命令启动：

```bash
# 使用 Maven 启动
mvn spring-boot:run

# 或者使用 Docker 启动
docker-compose up
```

### 2. 安装测试依赖

#### Python 测试（pytest）

```bash
pip install pytest requests
```

#### JavaScript 测试（Jest）

```bash
npm install axios jest
```

## 运行测试

### Python 测试

```bash
# 在项目根目录运行
pytest test_api.py -v
```

### JavaScript 测试

```bash
# 在项目根目录运行
npx jest test_api.js
```

## 测试内容

### 1. 公共接口测试

- **用户登录**：测试登录功能，获取 token
- **用户注册**：测试注册功能
- **自习室列表**：测试获取自习室列表
- **自习室详情**：测试获取自习室详情
- **座位列表**：测试获取座位列表
- **座位详情**：测试获取座位详情
- **公告列表**：测试获取公告列表
- **公告详情**：测试获取公告详情
- **规则列表**：测试获取规则列表
- **规则详情**：测试获取规则详情

### 2. 需要认证的接口测试

- **获取用户信息**：测试获取当前用户信息
- **获取我的预约列表**：测试获取用户的预约列表
- **获取签到记录**：测试获取用户的签到记录
- **获取当前签到状态**：测试获取用户的当前签到状态

## 测试结果

测试运行完成后，会显示每个测试用例的执行结果，包括通过、失败或跳过的测试。

## 注意事项

1. 确保后端服务已经启动，并且可以正常访问
2. 确保数据库已经初始化，并且有必要的测试数据
3. 测试过程中可能会创建临时用户，测试完成后可以手动清理
4. 如果测试失败，可以查看错误信息，定位问题所在
